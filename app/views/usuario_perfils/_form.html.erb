<%= form_with(model: usuario_perfil, local: true) do |form| %>
  <% if usuario_perfil.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(usuario_perfil.errors.count, "error") %> prohibited this usuario_perfil from being saved:</h2>

      <ul>
      <% usuario_perfil.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nome %>
    <%= form.text_field :nome %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
